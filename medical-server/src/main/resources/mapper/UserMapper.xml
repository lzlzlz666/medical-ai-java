<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.lz.mapper.UserMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into user ( username, password, nickname, avatar, phone, status, last_login_time, create_time, update_time )
        values
        ( #{username}, #{password}, #{nickname}, #{avatar}, #{phone}, #{status}, #{lastLoginTime}, #{createTime}, #{updateTime} )
    </insert>
</mapper>
